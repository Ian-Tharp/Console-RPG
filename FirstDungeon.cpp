#include "FirstDungeon.h"

//Constructor
FirstDungeon::FirstDungeon() {
    int numDoors = rand() % 10 + 1;
    int numMonsters = rand() % 10 + 3;

    for (int i = 0; i < numDoors; i++) {
        
    }
}